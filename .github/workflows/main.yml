name: CI Test Fail

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Simulate a failing test
        run: |
          echo "Running tests..."
          echo "Wait, something is wrong!"
          exit 1
